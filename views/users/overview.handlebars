<div class="user-detail">
    <h4>User ID: {{ currentUser._id }}</h4>
    <h4>First Name: {{ currentUser.first_name }}</h4>
    <h4>Last Name: {{ currentUser.last_name }}</h4>
    <h4>Department: {{ currentUser.department }}</h4>
    <h4>Email: {{ currentUser.email }}</h4>
    <h4>Sessions: {{ currentUser.session }}</h4>
    <h4>Assigned To: {{ currentUser.assigned_to }}</h4>
    <h4>Username: {{ currentUser.username }}</h4>
</div>